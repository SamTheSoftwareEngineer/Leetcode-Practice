#  Individal node on the linked list which points to the next node (or none if there is not a next node)
class Node:
    def __init__(self, data):
        self.data = data
        self.next = None

    

